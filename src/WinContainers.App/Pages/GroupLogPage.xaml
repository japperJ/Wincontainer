<Page
    x:Class="WinContainers_App.Pages.GroupLogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:WinContainers_App.ViewModels"
    xmlns:converters="using:WinContainers_App.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Back" Click="BackButton_Click" AutomationProperties.Name="Back" />

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{x:Bind ViewModel.GroupName, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}" TextTrimming="CharacterEllipsis" AutomationProperties.Name="Group name" />
                </StackPanel>

                <Border Grid.Column="2" Padding="8,2" VerticalAlignment="Center" CornerRadius="4" Background="{ThemeResource AccentFillColorDefaultBrush}">
                    <TextBlock Text="{x:Bind ViewModel.ContainerCount, Mode=OneWay}" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                </Border>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Padding="16,8">
            <Button x:Name="RefreshLogsButton" Content="Refresh" Click="RefreshLogsButton_Click" AutomationProperties.Name="Refresh logs" />
            <TextBlock Text="{x:Bind ViewModel.LogsInfoText, Mode=OneWay}" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" AutomationProperties.Name="Logs info" />
        </StackPanel>

        <ScrollViewer Grid.Row="2" x:Name="LogsScrollViewer" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Margin="16,0,16,16" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <TextBlock Text="{x:Bind ViewModel.LogsContent, Mode=OneWay}" FontFamily="Consolas" FontSize="12" Padding="12,8" TextWrapping="Wrap" IsTextSelectionEnabled="True" AutomationProperties.Name="Group logs" />
        </ScrollViewer>

        <Grid Grid.Row="2" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" Width="40" Height="40" />
                <TextBlock Text="{x:Bind ViewModel.LogsInfoText, Mode=OneWay}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
